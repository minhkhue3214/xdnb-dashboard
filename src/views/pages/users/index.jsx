import { memo } from 'react';
// project imports
import MainCard from '~/ui-component/cards/MainCard';

const UsersPage = () => <MainCard title="Users">UsersPage</MainCard>;

export default memo(UsersPage);
